import React from 'react'
import { <%= Name %> } from '../<%= Name %>'

describe('<%= Name %>', () => {
  let component

  beforeEach(() => {
    component = shallow(
      <<%= Name %> />
    )
  })

  it('should render', () => {
    expect(component.type()).to.eql('div')
  })
})
